{
  "name": "DVD Store Dev Container",
  "image": "mcr.microsoft.com/devcontainers/base:bullseye",
  "postCreateCommand": "apt-get update && apt-get install -y curl git && curl -fsSL https://deb.nodesource.com/setup_23.x | bash - && apt-get install -y nodejs && npm install",
  "forwardPorts": [3000],
  "remoteUser": "root",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "orta.vscode-jest"
      ]
    }
  }
}
